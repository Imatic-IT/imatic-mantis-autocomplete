> ⚠️ **This plugin is deprecated and no longer maintained.**
>
> The `@`-mention autocomplete functionality has moved into our
> [`imatic-mantis-formatting`](https://github.com/Imatic-IT/imatic-mantis-formatting)
> plugin, which is the supported successor. New installations should use that
> plugin instead. Existing issues will remain visible but new bug reports
> and feature requests should be opened against `imatic-mantis-formatting`.

# Imatic autocomplete

## Purpose

This plugin autocompletes mantis mentions. When user types `@` into text and some other character, list of suggestions opens if some username starts with the text.

## Installation

Copy all files into plugins/ImaticAutocomplete.
